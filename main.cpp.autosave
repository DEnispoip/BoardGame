#include "mainwindow.h"

#include <QApplication>

int main(int argc, char *argv[])
{
    QApplication a(argc, argv); // иницилизировали qt
    srand(time(NULL)); // 
    MainWindow w;
    w.show();
    return a.exec();
}
